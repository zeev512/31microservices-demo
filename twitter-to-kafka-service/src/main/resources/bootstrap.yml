# This bootstrap.yml file is used exclusively with config-server.
# If it doesn't start up for whatever reason then use application.yml configuration properties
# Normally application.yml file should be empty in order to let config-server
# load properties from git repository, which is pointed in bootstrap.yml for config-server application.
spring:
  application:
    name: twitter-to-kafka-service
  profiles:
    active: twitter_to_kafka
  cloud:
    config:
      name: twitter-to-kafka-service,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      # password: '{cipher}2ca52e09ec85aa620abce2e704242985bf786c9a684461b023a563b34b96c527ec81160eae48d731017c98ee0b9e1824'
      password: springCloud_Pwd!
#encrypt:
#  key:
#    'Demo_Pwd!2020'